name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install system audio libraries
        run: sudo apt-get install -y libportaudio2 portaudio19-dev libsndfile1

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Install package in editable mode
        run: pip install -e .

      - name: Lint with Ruff
        run: ruff check src/ tests/

      - name: Check formatting with Ruff
        run: ruff format --check src/ tests/

      - name: Run tests with coverage
        env:
          OPENAI_API_KEY: sk-test-fake-key-for-ci
          ANTHROPIC_API_KEY: sk-ant-test-fake-key-for-ci
          AI_PROVIDER: openai
        run: |
          pytest tests/ \
            --ignore=tests/test_audio.py \
            --cov=src \
            --cov-report=term-missing \
            --cov-report=xml \
            -v

      - name: Upload coverage report
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: coverage.xml
          fail_ci_if_error: false

  # mypy temporarily disabled -- internal error with current codebase
  # re-enable in Phase 4 once type stubs are stabilised
  # lint-types:
  #   name: Type Check (mypy)
